{{# obj.class == 'bucketEvents' }}
  {{#obj}}
    <ul class="visual">
    {{#events:eventIdx}}
      <li class="bucketEvent">
        <div class="bucketEvent_when">{{when}}</div>
        {{{ visualBucketEvent(.) }}}
      </li>
    {{/events}}
    </ul>
  {{/obj}}
{{/}}
