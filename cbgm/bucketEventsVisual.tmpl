{{# obj.class == 'bucketEvents' }}
  {{#obj}}
    <ul class="visual">
    {{#events:eventIdx}}
      <li class="bucketEvent">
        {{{ visualBucketEvent(.) }}}
      </li>
    {{/events}}
    </ul>
  {{/obj}}
{{/}}
