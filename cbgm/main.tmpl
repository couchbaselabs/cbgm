{{>main_header}}

<div class="container-fluid">
  <div class="row-fluid">
    <div class="span3">
      <div class="lhs">
        <h3>cbgm</h3>
        <div>
          <label>nodes (comma separated)</label>
          <input type="text" value="{{want.nodes}}"/>
          <label>numPartitions</label>
          <input type="text" value="{{want.numPartitions}}"/>
          <label>slaves</label>
          <input type="text" value="{{want.slaves}}"/>
        </div>
        <button class="btn btn-small" on-click="rebalance">
          rebalance
        </button>
      </div>
    </div>
    <div class="span7">
      <div class="rhs">
        {{>bucketEventsVisual}}
        {{#obj}}{{>__object}}{{/obj}}
        <pre>
          {{{objJSON}}}
        </pre>
      </div>
    </div>
  </div>
</div>

{{>main_footer}}
